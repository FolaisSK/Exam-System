spring:
  application:
    name: ExamSystem

  data:
    mongodb:
      uri: mongodb://localhost:27017/exam_system_db

server:
  port: 8080

jwt:
  secret: exam-system-super-secret-key
  expiration: 86400000  # 24 hours in milliseconds

logging:
  level:
    org.springframework.data.mongodb: DEBUG